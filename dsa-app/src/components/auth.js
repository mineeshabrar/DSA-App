import { auth } from "../config/firebase";
import {  } from "firebase/auth"

export const Auth = () => {
    return (
        <div>
            <button> Google Sign-in </button>
        </div>
    );
};